Assume that different countries will run distinct instances... right?
what about mobile
what about accessibility
for a particular installation, e.g. samoa, should language be switchable or fixed?
what roles should be defined
Can a user upload their own performance evaluation, or should it be their supervisor? If the supe, then reporting structure must be encoded in the database, right?
