User Stories
  User logs in and reaches the welcome page

Engineering Tasks
  Nav bar menus are in-place according to the video
  Admin can add users and configure user roles

Todo for current task
  ensure all login paths are tested
  roles and permissions control which nav elements are shown
  permissions have friendly names
  configure a rake task to run all tests, for webkit and also firefox
  look into simple_form gem for built-in hints and translations
  rake task to run i18n-tasks gem tests https://github.com/glebm/i18n-tasks
  check all validation error messages and flash messages for i18n translations
  it should not be possible to delete a role that is assigned to some user
  Provide a configurable timezone (hours offset from GMT) and use this to set all times on the UI
  check that all pages have a title configured
  Ensure that the actual application and author are cited in the LICENSE.txt file (near the end)
  Test facility to test email sending and receiving on production server

Security tasks
  Login timeout
  RackAttack install/test, with logging
  Login record
  Security test tool


good reference for optimizing test setup:
http://www.railsonmaui.com/tips/rails/capybara-phantomjs-poltergeist-rspec-rails-tips.html
good reference for dealing with timezones:
http://danilenko.org/2012/7/6/rails_timezones/
