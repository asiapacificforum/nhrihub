| {{#has_email }}
.inactive-icon.fa.fa-at( data-toggle='tooltip' title='{{i18n.tooltip_messages.email}}')
| {{/}}
| {{#has_phone }}
.class.inactive-icon.fa.fa-phone( data-toggle='tooltip' title='{{i18n.tooltip_messages.phone}}')
| {{/}}
| {{#has_letter }}
.class.inactive-icon.fa.fa-envelope-o( data-toggle='tooltip' title='{{i18n.tooltip_messages.letter}}')
| {{/}}
| {{#face_to_face }}
.class.inactive-icon.face_to_face( data-toggle='tooltip' title='{{i18n.tooltip_messages.face_to_face}}')
  .fa
  .fa
  .fa
| {{/}}

script.
  //import Ractive from 'ractive'
  export default Ractive.extend({
    template : $TEMPLATE,
    computed : {
          face_to_face() {
            return this.get('mode') == 'face to face';
          },
          has_email() {
            return this.get('mode') == 'email';
          },
          has_phone() {
            return this.get('mode') == 'phone';
          },
          has_letter() {
            return this.get('mode') == 'letter';
          }
        }
      }
    )
