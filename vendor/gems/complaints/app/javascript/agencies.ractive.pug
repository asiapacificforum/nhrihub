| {{#agency_ids}}
<agency id='{{.}}' />
| {{/agency_ids}}
| {{^agency_ids}}
div(style = 'padding-left:15px;')
  | {{ t.no_mandates }}
| {{/agency_ids}}

script.
  //import Ractive from 'ractive'
  import Agency from './agency'
  import translations from './translations.js'

  export default Ractive.extend({
    template : $TEMPLATE,
    components : {
      agency : Agency
    },
    data : function(){ return {
      t : translations.t('agencies')
    }},
  });
