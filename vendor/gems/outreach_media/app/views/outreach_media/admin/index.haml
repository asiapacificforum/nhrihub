.message_block
%h1=t('.heading')

%h4=t('.subheading.filetypes')
= render :partial => 'outreach_media_filetypes'

%h4=t('.subheading.filesize')
= render :partial => 'outreach_media_filesize'

%h4=t('.subheading.areas')
= render :partial => 'outreach_media_areas'

:coffeescript
  $ ->
    _.templateSettings = { interpolate: /\{\{(.+?)\}\}/g }

    window.flash_error_template = _.template($("#flash_error_template").html())


%script{:type => 'template/html', :id => :flash_error_template}
  %ul.error
    %li {{ message }}

%script{:type => 'page/notes'}
  :plain
  config params:
    positivity_defaults:
      "Reflects very negatively on the office"
      "Reflects slightly negatively on the office"
      "Has no bearing on the office"
      "Reflects slightly positively on the office"
      "Reflects very positively on the office"
    violation_severity_defaults:
      "Negligible"
      "Minor"
      "Moderate"
      "Significant"
      "Substantial"
      "Severe"
      "Critical"
      "Catastrophic"
