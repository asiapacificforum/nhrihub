%h1= t('.heading')

#media_appearances

= render partial: "outreach_media/media_appearances/media_appearance_templates"
= render :partial => 'reminders/reminder' # it's in the main app
= render :partial => 'notes/note' # it's in the main app
