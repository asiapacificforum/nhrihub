= stylesheet_link_tag 'internal_documents'
= render :partial => 'index'
= render :partial => 'shared/confirm_delete_modal'
= render :partial => 'reminders/reminder' # it's in the main app
= render :partial => 'shared/fileupload_buttonbar'

:coffeescript
  @maximum_filesize = #{IccReferenceDocument.maximum_filesize * 1000000}
  @permitted_filetypes = #{ IccReferenceDocument.permitted_filetypes }
  @files = #{ @icc_reference_documents.to_json }
  @flash_error = "#{t('.flash.no_files_error')}"
  @required_files_titles = #{@required_files_titles.to_json}
  @context = "icc"
  @delete_confirmation_message = "#{t('.delete_confirmation_message')}"

= render :partial => 'shared/confirm_delete_modal'
= javascript_include_tag "nhri/reference_documents"
